{
  "addressHelper": "allen_bradley",
  "connection": {
    "order": [
      "host", "port", "slot", "messageRouter", "connectTimeoutMs", "receiveTimeoutMs",
      "contextCheck", "readArrayUseSegment", "dataFormat"
    ]
  },
  "address": { "order": ["address"] }
}
