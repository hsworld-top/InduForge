# Product

## Register

product

## Users

InduForge 面向工业互联网应用交付链路中的方案人员、研发人员、实施人员、平台管理员、数据配置人员和运维人员。用户通常在桌面工作站或笔记本上连续处理工程创建、页面设计、数据接入、发布部署、节点维护和问题追踪任务。

这些用户需要一个能把设计、数据、控制、发布和运行串起来的平台，而不是分散的脚本、临时页面和手工交接文档。界面需要让专业用户快速定位任务、理解状态、完成配置，并在出错时知道下一步该做什么。

## Product Purpose

InduForge 是面向工业互联网场景的低代码开发平台，覆盖设计、数据接入、工程聚合、发布部署、节点运行与运维管理。平台目标是降低工业应用交付门槛，缩短从方案到可运行系统的路径，并降低研发、实施、数据和运维角色之间的协作成本。

产品分为五个长期层次：

- 设计层：`designer` 负责页面、组件、变量、绑定和交互定义。
- 平台数据层：`datacenter` 与 `data_service` 负责开发态数据域能力和数据工作台。
- 控制与管理层：`dev_core` 与 `dev_ide` 负责平台治理、工程管理、发布部署和运维管理。
- 共享运行内核层：负责开发态预览与运行态共用的页面解释、渲染、绑定和事件能力。
- 运行与节点层：负责节点侧访问入口、工程运行引擎、运行态数据服务和节点本地运维。

成功的产品体验应当让用户相信：工程状态可信、数据链路清楚、发布部署可控、运行问题可追踪。

## Brand Personality

InduForge 的默认产品性格是：专业、克制、可信。

界面语气应像可靠的工业控制台：清晰、稳定、有足够信息密度，但不嘈杂。它不追求消费级的惊喜，也不做夸张的品牌秀；它的价值来自把复杂链路拆成可理解、可操作、可验证的工作台。

## Anti-references

InduForge 不应看起来像以下类型：

- 营销型 SaaS 落地页：大面积 hero、空泛价值主张、装饰插画、重复卡片卖点。
- 炫技型暗色大屏：霓虹、过饱和渐变、发光边框、无意义动效。
- 低密度玩具化后台：过大的圆角、过多留白、信息承载不足。
- 一次性项目后台：控件风格混乱、颜色语义不统一、状态表达依赖临时硬编码。
- 纯设计器孤岛：子应用各自建立全局视觉语言，破坏平台一体性。

## Design Principles

1. 工作台优先：首屏直接进入任务，不做营销入口。
2. 状态可信：运行、部署、权限、数据和节点状态必须稳定可见，不能只靠隐式状态。
3. 一体化但分域：`dev_ide`、`designer`、`datacenter` 应共享视觉语言，但保留各自领域任务结构。
4. 密度服务效率：界面应支持连续操作和快速扫描，而不是用低信息密度换取表面精致。
5. 可维护的美感：颜色、圆角、阴影、组件和动效应通过 token 与规范沉淀，减少页面级硬编码。

## Accessibility & Inclusion

InduForge 默认以产品工具的可用性为底线：

- 自定义可点击元素应使用语义化 `button` 或可访问控件。
- 图标按钮必须提供 tooltip 或 `aria-label`。
- 状态不能只依赖颜色表达，应配合文本、标签或图标。
- 焦点态必须可见，键盘操作不应丢失上下文。
- 动效应服务状态反馈，并逐步支持 `prefers-reduced-motion`。
- 管理、部署、删除、批量操作等高风险流程必须提供清晰确认、禁用态和错误反馈。
