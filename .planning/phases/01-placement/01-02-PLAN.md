---
phase: 01-placement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - designer/src/editor-core/utils/placement-resolver.ts
autonomous: true
requirements:
  - TECH-01
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "placementResolver.ts is the single entry for all container hit detection"
    - "Strategy pattern handles flex/free/grid container types separately"
    - "Root-level container is primary target, depth-first is fallback (D-01, D-02)"
    - "Y coordinate first sorting with X secondary (D-04)"
    - "Append fallback when insertIndex is stale (D-05)"
    - "Nearest neighbor fine-tuning after Y+X sorting (D-06)"
  artifacts:
    - path: "designer/src/editor-core/utils/placement-resolver.ts"
      provides: "Single entry placement resolution with Strategy pattern"
      min_lines: 150
      contains: "PlacementStrategy"
    - path: "designer/src/editor-core/utils/placement-resolver.test.ts"
      provides: "Unit tests for placement resolver strategies"
      min_lines: 80
      contains: "describe.*placementResolver"
  key_links:
    - from: "DragDropManager.ts"
      to: "placement-resolver.ts"
      via: "will delegate to new resolver"
      pattern: "resolveDropTarget"
    - from: "use-node-drop.ts"
      to: "placement-resolver.ts"
      via: "will use new resolver"
      pattern: "resolveDropContainer"
---

<objective>
抽取 placementResolver.ts 单一入口，使用 Strategy 模式处理 flex/free/grid 布局类型，实现 D-01~D-06 的放置规则。
</objective>

<execution_context>
@D:/SVNCode/indu-forge/.claude/get-shit-done/workflows/execute-plan.md
@D:/SVNCode/indu-forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@designer/src/editor-core/utils/placement-utils.ts
@designer/src/ui/editors/page/canvas/services/DragDropManager.ts
@designer/src/ui/editors/page/canvas/composables/use-node-drop.ts
@.planning/phases/01-placement/01-CONTEXT.md
@.planning/phases/01-placement/01-RESEARCH.md
</context>

<interfaces>
<!-- From DragDropManager.ts - existing types we will wrap -->
export type FlowContainerKind = "flex" | "grid" | "free";

export interface DropTargetResolution {
  parentId: string | null;
  index: number;
  dropPosition: { x: number; y: number } | null;
  containerType: FlowContainerKind | null;
}

export interface FlexInsertLine {
  orientation: "horizontal" | "vertical";
  offset: number;
}

export interface FlexInsertResult {
  index: number;
  position: "before" | "after" | "inside";
  insertLine: FlexInsertLine | null;
}

export interface GridCellHint {
  orientation: string;
  row: number;
  col: number;
  highlightRect: { left: number; top: number; width: number; height: number };
}
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 PlacementStrategy 接口和策略基类</name>
  <files>designer/src/editor-core/utils/placement-resolver.ts, designer/src/editor-core/utils/placement-resolver.test.ts</files>
  <behavior>
    - Test 1: FlexContainerStrategy has containerKind = 'flex'
    - Test 2: FreeContainerStrategy has containerKind = 'free'
    - Test 3: GridContainerStrategy has containerKind = 'grid'
    - Test 4: Each strategy implements resolveInsertIndex()
    - Test 5: Each strategy implements canAcceptChild()
  </behavior>
  <action>
    创建 placement-resolver.ts，实现 Strategy 模式：

    1. 定义 PlacementStrategy 接口：
    ```typescript
    export interface PlacementStrategy {
      readonly containerKind: 'flex' | 'free' | 'grid';
      canAcceptChild(parentNode: ComponentNode, childType: string): boolean;
      resolveInsertIndex(
        event: DragEvent,
        containerNode: ComponentNode,
        containerElement: HTMLElement,
        zoom: number
      ): number;
      resolveDropPosition?(
        event: DragEvent,
        containerElement: HTMLElement,
        zoom: number
      ): { x: number; y: number } | null;
    }
    ```

    2. 创建抽象基类 ContainerStrategy 实现公共逻辑：
    - Y-first 排序（D-04）
    - 最近邻精调（D-06）
    - insertIndex 验证和 append 兜底（D-05）

    3. 实现三个策略类：
    - FlexContainerStrategy: 使用 DragDropManager.calculateFlexInsertPosition 逻辑
    - FreeContainerStrategy: 始终 append 到 children 末尾
    - GridContainerStrategy: 使用 DragDropManager.calculateGridCell 逻辑

    4. 导出 resolvePlacement 单一入口函数
  </action>
  <verify>
    <automated>pnpm --dir designer test --run -- placement-resolver.test.ts</automated>
  </verify>
  <done>placementResolver.ts 定义了 Strategy 接口和三个策略实现</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现容器命中检测（根级优先 + 深度优先 fallback）</name>
  <files>designer/src/editor-core/utils/placement-resolver.ts, designer/src/editor-core/utils/placement-resolver.test.ts</files>
  <behavior>
    - Test 1: resolvePlacement returns root-level container when preferRootLevel=true (D-01)
    - Test 2: resolvePlacement falls back to depth-first when root-level unclear (D-02)
    - Test 3: Only direct children of root container are considered when rootLevel=true
  </behavior>
  <action>
    实现 resolvePlacement 函数中的容器命中检测逻辑：

    1. 实现 findTargetContainer 函数：
    - 使用 document.elementFromPoint 获取鼠标下的元素
    - 遍历获取 data-node-id 的祖先元素
    - D-01: 优先返回根级容器的直接子级
    - D-02: 当根级容器逻辑不明确时，降级使用深度优先

    2. 根据容器类型选择对应策略：
    - 使用 getChildPositioning(nodeType) 判断 flow/absolute
    - 使用 isContainerType(nodeType) 判断是否为容器

    3. 集成 DragDropManager 的现有逻辑（不要重复造轮子）
  </action>
  <verify>
    <automated>pnpm --dir designer test --run -- placement-resolver.test.ts</automated>
  </verify>
  <done>容器命中检测实现根级优先 + 深度优先 fallback</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 Y-first + append fallback + nearest neighbor insertIndex 计算</name>
  <files>designer/src/editor-core/utils/placement-resolver.ts, designer/src/editor-core/utils/placement-resolver.test.ts</files>
  <behavior>
    - Test 1: siblings sorted by Y then X (D-04)
    - Test 2: insertIndex returns siblings.length when target doesn't exist (D-05)
    - Test 3: nearest neighbor adjustment after initial Y+X sort (D-06)
  </behavior>
  <action>
    实现 calculateInsertIndex 函数的完整算法：

    1. Y-first + X辅助排序（D-04）：
    ```typescript
    const sorted = [...siblings].sort((a, b) => {
      const posA = getNodeVisualTop(a);
      const posB = getNodeVisualTop(b);
      return posA.y - posB.y || posA.x - posB.x;
    });
    ```

    2. 找到最近的邻居节点（D-06）：
    - 计算 dropPos 与所有 siblings 的 Y+X 距离
    - 选择距离最小的作为参考

    3. Append 兜底（D-05）：
    ```typescript
    if (!isValidIndex(nearestIdx, siblings)) {
      return siblings.length; // append
    }
    ```
  </action>
  <verify>
    <automated>pnpm --dir designer test --run -- placement-resolver.test.ts</automated>
  </verify>
  <done>Y-first + append fallback + nearest neighbor 实现完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> placement-resolver | Untrusted DOM events cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | placement-resolver.ts | mitigate | Validate containerNode exists before accessing children |
| T-01-02-02 | Denial | insertIndex calculation | mitigate | Bounds check before returning index |
</threat_model>

<verification>
pnpm --dir designer typecheck
pnpm --dir designer test --run -- placement-resolver.test.ts
</verification>

<success_criteria>
1. placementResolver.ts 是唯一处理容器命中检测的文件
2. Strategy 模式正确处理 flex/free/grid 三种容器类型
3. insertIndex 计算遵循 Y-first + append fallback + nearest neighbor
</success_criteria>

<output>
After completion, create `.planning/phases/01-placement/01-02-SUMMARY.md`
</output>
