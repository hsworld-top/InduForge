---
phase: 01-placement
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - designer/src/ui/editors/page/canvas/CanvasInsertLineOverlay.vue
  - designer/src/ui/editors/page/canvas/DropIndicator.vue
  - designer/src/ui/editors/page/canvas/services/DragDropManager.ts
  - designer/src/ui/editors/page/canvas/composables/use-node-drop.ts
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09

must_haves:
  truths:
    - "Ghost drag shadow shows at cursor position during drag (D-07)"
    - "Insert indicator line displays at correct insert position (D-08)"
    - "Container highlight uses dashed border (not solid) to distinguish from selection (D-09)"
    - "Insert line appears beside nearest neighbor, not overlapping nodes"
  artifacts:
    - path: "designer/src/ui/editors/page/canvas/DropIndicator.vue"
      provides: "Ghost drag shadow rendering"
      contains: "ghost\|opacity"
    - path: "designer/src/ui/editors/page/canvas/CanvasInsertLineOverlay.vue"
      provides: "Insert line indicator rendering"
      contains: "insertLine\|offset"
    - path: "designer/src/ui/editors/page/canvas/services/DragDropManager.ts"
      provides: "Container highlight logic"
      contains: "highlight\|dashed"
  key_links:
    - from: "use-node-drop.ts"
      to: "DropIndicator.vue"
      via: "show/hide ghost based on dragState"
      pattern: "showInsertLine"
    - from: "DragDropManager"
      to: "CanvasInsertLineOverlay"
      via: "set insertLineStyle based on dropDecision"
      pattern: "insertLineStyle"
---

<objective>
实现拖拽视觉反馈：Ghost 拖影（D-07）、插入指示线（D-08）、容器高亮（D-09）。
</objective>

<execution_context>
@D:/SVNCode/indu-forge/.claude/get-shit-done/workflows/execute-plan.md
@D:/SVNCode/indu-forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@designer/src/ui/editors/page/canvas/composables/use-node-drop.ts
@designer/src/ui/editors/page/canvas/services/DragDropManager.ts
@designer/src/ui/editors/page/canvas/CanvasInsertLineOverlay.vue
@designer/src/ui/editors/page/canvas/DropIndicator.vue
@.planning/phases/01-placement/01-CONTEXT.md
@.planning/phases/01-placement/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 Ghost 拖影 (D-07)</name>
  <files>designer/src/ui/editors/page/canvas/DropIndicator.vue</files>
  <action>
    增强 DropIndicator.vue 实现 Ghost 拖影：

    1. Ghost 拖影规范（Claude's Discretion）：
       - 透明度 0.5（opacity: 0.5）
       - 使用 pointer-events: none 避免干扰拖放
       - 显示组件的半透明轮廓

    2. 实现方式：
       - 在 DropIndicator 中添加 ghost 状态
       - 当 isDragging && dragType 时显示 ghost
       - Ghost 位置跟随鼠标（使用 placementResolver 返回的 dropPosition）

    3. 样式：
       - background: rgba(0, 0, 0, 0.5)
       - border: 1px dashed #666
       - border-radius: 4px
  </action>
  <verify>
    <automated>pnpm --dir designer typecheck</automated>
  </verify>
  <done>Ghost 拖影显示在鼠标位置，opacity 0.5</done>
</task>

<task type="auto">
  <name>Task 2: 实现插入指示线 (D-08)</name>
  <files>designer/src/ui/editors/page/canvas/CanvasInsertLineOverlay.vue, designer/src/ui/editors/page/canvas/composables/use-node-drop.ts</files>
  <action>
    增强 CanvasInsertLineOverlay.vue 实现插入指示线：

    1. 插入指示线规范（D-08 + Specific Ideas）：
       - 在放置位置显示插入线（横线/竖线）
       - 插入线应该在最近邻节点旁边显示，而不是覆盖在节点上
       - 颜色和线宽由 Claude's Discretion 确定

    2. 实现方式：
       - 使用 placementResolver 返回的 insertLineHint
       - 在 CanvasInsertLineOverlay 中渲染 insertLine
       - insertLineStyle 包含 orientation (horizontal/vertical) 和 offset

    3. 样式（Claude's Discretion）：
       - 颜色: #409EFF (Element Plus primary blue)
       - 线宽: 2px
       - 长度: 覆盖目标容器宽度/高度
  </action>
  <verify>
    <automated>pnpm --dir designer typecheck</automated>
  </verify>
  <done>插入指示线正确显示在最近邻节点旁边</done>
</task>

<task type="auto">
  <name>Task 3: 实现容器高亮 (D-09)</name>
  <files>designer/src/ui/editors/page/canvas/services/DragDropManager.ts, designer/src/ui/editors/page/canvas/composables/use-node-drop.ts</files>
  <action>
    实现容器高亮逻辑：

    1. 容器高亮规范（D-09 + Specific Ideas）：
       - 目标容器在拖拽进入时高亮显示
       - 使用虚线边框，与选中态实线边框区分
       - 视觉区分放置目标

    2. 实现方式：
       - 在 DragDropManager.calculateDropDecision 中设置容器高亮状态
       - 在 use-node-drop.ts 中当 dragover 进入容器时设置 isDragOver
       - 使用 CSS 类或内联样式添加虚线边框

    3. 样式（Claude's Discretion）：
       - border: 2px dashed #409EFF
       - background: rgba(64, 158, 255, 0.1)
  </action>
  <verify>
    <automated>pnpm --dir designer typecheck</automated>
  </verify>
  <done>目标容器在 dragover 时显示虚线边框高亮</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> DropIndicator | DOM overlay cross here |
| client -> CanvasInsertLineOverlay | DOM overlay cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Denial | Ghost rendering | accept | Low impact, only visual feedback |
| T-01-04-02 | Denial | Insert line rendering | accept | Low impact, only visual feedback |
</threat_model>

<verification>
pnpm --dir designer typecheck
</verification>

<success_criteria>
1. Ghost 拖影显示在鼠标位置，opacity 0.5
2. 插入指示线正确显示在最近邻节点旁边
3. 目标容器在 dragover 时显示虚线边框高亮
</success_criteria>

<output>
After completion, create `.planning/phases/01-placement/01-04-SUMMARY.md`
</output>
