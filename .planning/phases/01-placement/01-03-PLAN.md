---
phase: 01-placement
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - designer/src/ui/editors/page/canvas/DesignCanvas.vue
  - designer/src/ui/editors/page/canvas/NodeRenderer.vue
  - designer/src/ui/editors/page/canvas/composables/use-node-drop.ts
autonomous: true
requirements:
  - PLACE-01
  - PLACE-03

must_haves:
  truths:
    - "DesignCanvas.handleCanvasDrop delegates to placementResolver"
    - "NodeRenderer.handleDrop delegates to placementResolver"
    - "insertIndex staleness fixed: target doesn't exist triggers append fallback (D-05)"
    - "Fast drag-drop: insertIndex validated before use"
  artifacts:
    - path: "designer/src/ui/editors/page/canvas/DesignCanvas.vue"
      provides: "Canvas-level drop handler wired to placementResolver"
      contains: "placementResolver"
    - path: "designer/src/ui/editors/page/canvas/composables/use-node-drop.ts"
      provides: "Node-level drop handler wired to placementResolver"
      contains: "placementResolver"
  key_links:
    - from: "DesignCanvas.vue"
      to: "placement-resolver.ts"
      via: "import and call resolvePlacement"
      pattern: "resolvePlacement"
    - from: "use-node-drop.ts"
      to: "placement-resolver.ts"
      via: "import and call resolvePlacement"
      pattern: "resolvePlacement"
---

<objective>
将 DesignCanvas.handleCanvasDrop 和 NodeRenderer.handleDrop（通过 use-node-drop）统一收敛到 placementResolver，解决 insertIndex 过期问题（PLACE-03）。
</objective>

<execution_context>
@D:/SVNCode/indu-forge/.claude/get-shit-done/workflows/execute-plan.md
@D:/SVNCode/indu-forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@designer/src/ui/editors/page/canvas/DesignCanvas.vue
@designer/src/ui/editors/page/canvas/NodeRenderer.vue
@designer/src/ui/editors/page/canvas/composables/use-node-drop.ts
@designer/src/editor-core/utils/placement-resolver.ts
@.planning/phases/01-placement/01-CONTEXT.md
@.planning/phases/01-placement/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 DesignCanvas.handleCanvasDrop 使用 placementResolver</name>
  <files>designer/src/ui/editors/page/canvas/DesignCanvas.vue</files>
  <action>
    重构 DesignCanvas.vue 中的 handleCanvasDrop 函数：

    1. 导入 placementResolver 的 resolvePlacement 函数
    2. 替换现有的 resolveLayoutFromPoint + 手动 insertNode 逻辑
    3. 调用 resolvePlacement(event, canvasEl, doc, currentPage, zoomValue)
    4. 使用返回的 parentId、index、dropPosition 进行 insertNode

    原有逻辑（需要替换）:
    - resolveLayoutFromPoint() 手动查找 ElLayout
    - insertIntoElLayout() 手动处理 ElLayout auto-insert
    - 直接调用 insertNode

    新逻辑:
    - 使用 placementResolver.resolvePlacement 统一处理
    - 保留 ElLayout auto-insert 逻辑（因为这是特殊业务规则）
  </action>
  <verify>
    <automated>pnpm --dir designer typecheck</automated>
  </verify>
  <done>DesignCanvas.handleCanvasDrop 使用 placementResolver</done>
</task>

<task type="auto">
  <name>Task 2: 重构 use-node-drop.ts 使用 placementResolver</name>
  <files>designer/src/ui/editors/page/canvas/composables/use-node-drop.ts</files>
  <action>
    重构 use-node-drop.ts 中的拖放逻辑：

    1. 导入 placementResolver 的 resolvePlacement 函数
    2. 替换 resolveDropContainer 函数，使用 resolvePlacement
    3. 替换 handleDragOver 中的 resolveLayoutInsertFromPoint，使用 resolvePlacement
    4. 确保 insertIndex staleness 问题通过 D-05 append fallback 修复

    关键点：
    - use-node-drop.ts 已有复杂的 Tabs/Collapse/scoped slot 处理
    - 需要保留这些业务逻辑，只替换底层的 placement 逻辑
    - resolveScopedSlotMeta 和 mapScopedInsertIndex 保持不变
  </action>
  <verify>
    <automated>pnpm --dir designer typecheck</automated>
  </verify>
  <done>use-node-drop.ts 使用 placementResolver</done>
</task>

<task type="auto">
  <name>Task 3: 验证 insertIndex staleness fix (PLACE-03)</name>
  <files>designer/src/editor-core/utils/placement-resolver.ts</files>
  <action>
    验证 insertIndex staleness 问题已修复：

    1. 在 placementResolver.resolveInsertIndex 中确保 D-05 实现：
       - 当 targetIndex 指向的节点不存在时，fallback 到 append

    2. 在调用 insertNode 之前验证 index 有效性：
       - 检查 index 是否在 [0, children.length] 范围内
       - 检查 index 指向的节点是否仍然存在

    3. 添加集成测试模拟快速拖拽场景：
       - 模拟 siblings 在 dragover 和 drop 之间变化
       - 验证最终插入位置正确
  </action>
  <verify>
    <automated>pnpm --dir designer test --run -- placement-resolver.test.ts</automated>
  </verify>
  <done>insertIndex staleness 问题通过 append fallback 修复</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> DesignCanvas | Drag events with asset payload cross here |
| client -> NodeRenderer | Drag events with node payload cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | DesignCanvas.handleCanvasDrop | mitigate | Validate componentType before insertNode |
| T-01-03-02 | Tampering | use-node-drop handleDrop | mitigate | Validate parentId exists before insertNode |
</threat_model>

<verification>
pnpm --dir designer typecheck
pnpm --dir designer test --run -- --grep "placement\|insertIndex" 2>/dev/null || echo "No matching tests yet"
</verification>

<success_criteria>
1. DesignCanvas.handleCanvasDrop 使用 placementResolver
2. use-node-drop.ts 使用 placementResolver
3. insertIndex staleness 问题通过 append fallback 修复
4. 组件可以在 flex/free/grid 容器中放置成功
</success_criteria>

<output>
After completion, create `.planning/phases/01-placement/01-03-SUMMARY.md`
</output>
