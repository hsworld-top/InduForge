# InduForge 仓库协作规则

本文件定义仓库级规则；具体模块遵守路径中更近的 `AGENTS.md`。工程、数据库或交付流程涉及详细约定时，查阅 [开发规范](docs/05-研发与交付/开发规范.md) 的相关章节。

友好沟通，围绕根因选择简单、可靠、易维护的方案。反复出现的问题应重新审视架构；发现规则不再适用时，说明依据并提出调整建议。

## 工作方式

- 默认使用简体中文交流；按任务核实相关代码、脚本和正式契约，不要求每次改动先通读整套文档。
- 简单、明确且低风险的改动直接实现并验证。复杂、风险高或需求不清的任务，先给出“做什么 -> 怎么验证”的简短计划，取得确认后再编码。已确认的方案和授权持续有效；范围内的实现、修复和必要验证继续完成，仅新增关键决策、超出授权或无法安全处理的冲突需要再次询问。
- 修复缺陷先复现，优先复用已有失败用例；有覆盖缺口时补充最小回归测试，再修复根因并验证受影响范围。无法自动化复现时保留可重复步骤。新增功能先明确成功标准，再实现并验证相关路径。
- 只修改当前任务必要的文件，匹配现有风格；不顺手重构、格式化或删除无关内容。
- 工作区可能有用户未提交的改动。不得回滚、覆盖或格式化无关改动；冲突或影响不明时先说明。
- 开发初期不维护历史接口、页面、数据库结构或数据的兼容分支。需要删除现有接口、结构或逻辑时，先说明影响并取得确认。
- 核心逻辑使用中文注释说明意图、输入输出和异常处理；自解释的简单代码与样式不强制注释。

## 仓库约定

- 默认从仓库根目录执行命令。Windows 优先使用 PowerShell、cmd、模块脚本或 Makefile。
- Node 依赖和脚本统一使用根目录 pnpm workspace：仅在根目录执行 `pnpm install`，仅提交根目录 `pnpm-lock.yaml`。按变更模块选择 `package.json` 中对应的检查入口；根 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm format:check` 是聚合命令，覆盖范围以脚本为准。
- 平台开发环境变量使用根目录 `.env`；调用 `dev:env` 的启动入口会在文件缺失时从 `.env.development.example` 生成。Pi Web 等容器组件使用各自部署配置约定。端口以环境模板和 [环境端口规划](docs/06-运维与安全/环境端口规划.md) 为准。
- 正式设计、契约和研发规范放在 `docs/`；临时过程记录默认保留在任务对话中，不作为正式架构依据。除非任务明确点名，不修改 `scripts/docker/` 或 `scripts/nginx/`。
- 不创建、合并或删除分支，除非用户明确要求。提交信息使用中文，推荐 `type(scope): subject`；不得提交本地产物、日志或环境文件。

## 强制约束

- `dev_core`、`data_service` 的对外 API 遵守 [统一 REST 接口规范与清单](docs/04-契约与规范/统一REST接口规范与清单.md)：前缀 `/api/v1`、健康检查 `/health`、JSON 响应包络为 `code`、`msg`、`data`、`reqId`，且仅 `code = 0` 表示成功。流式响应和其他组件接口遵守各自正式契约。
- 列表接口由后端提供分页、搜索和排序；前端对可增长数据按页或按需加载。批量写入优先使用批量接口与事务，批量查询大量 ID 使用请求体或专用接口。
- 数据库操作必须参数化。数据库结构只维护“从零创建当前最终结构”的基线；不得新增迁移、回滚、回填或启动时修复逻辑。具体操作和验证要求见 [数据库设计](docs/04-契约与规范/数据库设计.md)。
- 新增错误码前先复用现有规则；确有必要时同步更新 [统一错误码枚举表](docs/04-契约与规范/统一错误码枚举表.md)。

## 交付要求

- 修改后执行与变更面最接近的静态检查、编译或测试；TypeScript 改动必须通过相应类型检查。修复当前变更引起的失败并复查受影响范围，必要验证完成后再交付；无新增改动或风险时不重复全量检查。
- 未经用户明确要求，不得自主启动、重启或保持运行任何业务项目、前端开发服务器或本地服务进程。接口验证优先使用用户已启动的服务；没有可用服务时，说明未验证原因，不得为验证而自行启动项目。
- 核实测试只使用临时目录、本地模拟依赖且自动清理后，可直接运行；该授权不包括连接共享环境或启停业务服务。
- 完成时说明修改范围、已执行验证、未验证项和剩余风险；无法验证时说明原因，并列出建议人工验收的成功与失败路径。
